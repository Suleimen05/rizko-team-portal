{
  "name": "rizko-team-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "install-server": "cd server && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
