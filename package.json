{
  "name": "rizko-team-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
